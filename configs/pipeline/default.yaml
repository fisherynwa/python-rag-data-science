# configs/pipeline/default.yaml
_target_: src.pipeline.RAGPipeline
model: "qwen2.5:1.5b"
temperature: 0.1
top_k: 3
alpha: 0.75
num_predict: 1000
system_prompt: >
  You are a Python expert. Answer the question using only the context below.
  Always include a working code example.
  If the context is not relevant, say so - do not invent answers.
  Context: {context}